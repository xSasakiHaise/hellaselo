The build now pulls HellasControl `2.0.0` from JitPack via Gradle. Dropping `hellascontrol-2.0.0.jar` into this
directory remains optional for offline builds or when you want to override the dependency locally.
